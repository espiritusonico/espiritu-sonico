---
const { nombre, descripcion, imagen, link } = Astro.props;
---

<div class="bg-violetmid rounded-xl p-6 shadow-md text-left">
  <img src={imagen} alt={nombre} class="w-full h-48 object-cover rounded-md mb-4" />
  <h2 class="text-2xl font-semibold mb-2">{nombre}</h2>
  <p class="text-base leading-relaxed mb-4">{descripcion}</p>
  <a href={link} class="inline-block text-sm underline hover:text-gold/70 transition">
    Explorar categoría
  </a>
</div>
